import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Else = () => {
  return (
    <View>
      <Text>Else</Text>
    </View>
  )
}

export default Else

const styles = StyleSheet.create({})